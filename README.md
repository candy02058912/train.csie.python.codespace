# train.csie.python.codespace

This repository includes a minimal GitHub Codespaces devcontainer that installs
[`uv`](https://github.com/astral-sh/uv) during setup, so `uv` is available when
the Codespace starts.